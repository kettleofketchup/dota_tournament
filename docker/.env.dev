NODE_ENV="dev"
RELEASE=false
DEBUG=true
TEST=false
DOMAIN=localhost
FRONTEND_HOST=localhost
FRONTEND_PORT=3000
BACKEND_HOST=localhost
BACKEND_PORT=8000

# SSR API URL - used by frontend during server-side rendering
# Default: http://backend:8000/api (Docker Compose service name)
# Override for different deployments (K8s, external backend, etc.)
SSR_API_URL=http://backend:8000/api
